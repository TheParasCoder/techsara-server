# Rebuild marker: 2026-06-03 — force Render to redeploy from latest main
services:
  - type: web
    name: techsara-signaling-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
